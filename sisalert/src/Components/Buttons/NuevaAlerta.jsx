import React, { Component } from 'react';

class NuevaAlerta extends React.Component{
    render(){
        return (
            <button className="primaryButton">
                Nueva alerta
            </button>
        );
    }
}

export default NuevaAlerta;